<Window x:Class="Greenhose.CompleteTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Greenhose"
        mc:Ignorable="d"
        Title="CompleteTaskWindow" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Описание задачи:" Grid.Row="0" Margin="0,0,0,5" FontWeight="Bold"/>
        <TextBlock x:Name="TaskDescriptionText" Grid.Row="1" Margin="0,0,0,15" TextWrapping="Wrap"/>

        <TextBlock Text="Комментарий к выполнению:" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="CompletionCommentsTextBox" Grid.Row="3" Height="80" TextWrapping="Wrap" 
                VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CompleteButton" Content="Выполнено" Width="100" Height="30" 
                   Margin="0,0,10,0" Background="#4CAF50" Foreground="White" Click="CompleteButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="30" 
                   Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
