<Window x:Class="Greenhose.EditPlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Greenhose"
        mc:Ignorable="d"
        Title="EditPlanWindow" Height="450" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Теплица:" Grid.Row="0" Margin="0,0,0,5"/>
        <ComboBox x:Name="GreenhouseComboBox" Grid.Row="1" Margin="0,0,0,15" Height="28" VerticalContentAlignment="Center"/>

        <TextBlock Text="Дата начала:" Grid.Row="2" Margin="0,0,0,5"/>
        <DatePicker x:Name="StartDatePicker" Grid.Row="3" Margin="0,0,0,15" Height="28"/>

        <TextBlock Text="Дата окончания:" Grid.Row="4" Margin="0,0,0,5"/>
        <DatePicker x:Name="EndDatePicker" Grid.Row="5" Margin="0,0,0,15" Height="28"/>

        <TextBlock Text="Статус:" Grid.Row="6" Margin="0,0,0,5"/>
        <ComboBox x:Name="StatusComboBox" Grid.Row="7" Margin="0,0,0,0" Height="28" VerticalContentAlignment="Center">
            <ComboBoxItem Content="Активный"/>
            <ComboBoxItem Content="Завершен"/>
            <ComboBoxItem Content="Отменен"/>
        </ComboBox>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Height="30" 
                    Margin="0,0,10,0" Click="SaveButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="30" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>