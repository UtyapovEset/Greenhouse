<Window x:Class="Greenhose.AddPlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Greenhose"
        mc:Ignorable="d"
        Title="AddPlanWindow" Height="400" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Теплица:" Grid.Row="0" Margin="0,0,0,5"/>
        <ComboBox x:Name="GreenhouseComboBox" Grid.Row="1" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Дата начала:" Grid.Row="2" Margin="0,0,0,5"/>
        <DatePicker x:Name="StartDatePicker" Grid.Row="3" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Дата окончания:" Grid.Row="4" Margin="0,0,0,5"/>
        <DatePicker x:Name="EndDatePicker" Grid.Row="5" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Статус:" Grid.Row="6" Margin="0,0,0,10"/>
        <ComboBox x:Name="StatusComboBox" Grid.Row="7" Margin="0,0,0,50" Height="25">
            <ComboBoxItem Content="Активный" IsSelected="True"/>
            <ComboBoxItem Content="Завершен"/>
            <ComboBoxItem Content="Отменен"/>
        </ComboBox>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Height="30" 
                   Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="30" 
                   Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>